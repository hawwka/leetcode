``` Java
 /*
    Решение через Sliding Windows + HashMap

    Шаг 1. 
    Заполняем две мапы на размер s1.length() - 1 первую мапу символами из s1, вторую из s2
    Если мапы равны, return true;
    Шаг 2. 
    Создаем скользящее окно.
    Тк начальная позиция окна уже проверена, то right = s1.length() (а не s1.length - 1) те сдвиг на элемент вправо.
    Инкрементируем значение символа в по правому указателю s1Count
    Декрементируем символ на который указывает левый указатель
    (Если кол-во повторений символа по левому указателю стал 0 в мапе, то удаляем его из s2) ->
    -> эта проверка нужна, чтобы не удалить только что добавленный элемент правым указателем, а 
    удаляем элемент, чтобы поддержать размерность s1Count и s2Count одинаковой.

    Делаем проверку s1Count и s2Count => s1Count.equals(s2Count)
    если true, то вернем true
    
    Если false, то продолжим цикл либо пока rigth < s2.length(), либо пока !s1Count.equals(s2Count)
*/

class Solution {
    public boolean checkInclusion(String s1, String s2) {
        
    }
}
```
